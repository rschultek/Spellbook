/**
 * Vitest Global Setup
 *
 * This file runs before all tests and sets up the testing environment.
 */

// Currently no global setup needed for pure utility function tests
// Add custom matchers or global setup here as needed
